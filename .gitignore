# ==== OS / IDE ====
.DS_Store
Thumbs.db
*.suo
*.user
*.userosscache
*.sln.docstates
.idea/               # Rider
.vscode/*            # ignore local VS Code; keep shared settings if needed
!.vscode/extensions.json
!.vscode/settings.json

# ==== .NET (server) ====
[Bb]in/
[Oo]bj/
[Ll]og/
[Ll]ogs/
[Tt]est[Rr]esult*/
[Bb]uild[Ll]og.*
*.binlog
artifacts/
project.lock.json
project.fragment.lock.json
ScaffoldingReadMe.txt
# NuGet
*.nupkg
*.snupkg

# ==== ASP.NET Core ====
# Keep appsettings*.json committed but NEVER put secrets in them
# Use dotnet user-secrets or env vars for secrets
# Publish output
**/publish/

# ==== Node / Web client ====
node_modules/
dist/
.build/
.cache/
coverage/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-lock.yaml   # (optional: commit a single lockfile you actually use)
# Angular
.angular/
# React/Vite/Next
.next/
out/

# ==== Env & local config ====
.env
.env.*.local
*.local.json

# ==== Docker ====
# Keep a committed docker-compose.yml; ignore local overrides
docker-compose.override.yml

# ==== Generated code (optional) ====
# If you auto-generate OpenAPI clients, ignore them unless you plan to commit
client/src/generated/
contracts/gen/
